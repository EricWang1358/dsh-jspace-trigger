# Supply-chain hardening for a dependency-free plugin.
# Install scripts are disabled so no third-party package can run arbitrary code
# during `npm install`. The runtime ships zero npm dependencies, so this costs
# nothing and prevents a compromised devDependency from executing postinstall.
ignore-scripts=true
audit=true
fund=false
